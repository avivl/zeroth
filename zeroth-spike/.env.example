# Anthropic auth is API key only (ADR-Z-0008). Copy to .env and fill in.
# Never commit .env. The spike reads the key from the environment and
# never writes it to disk, logs, or HTTP responses.
ANTHROPIC_API_KEY=
