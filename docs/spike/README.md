# Spikes

Time-boxed investigations. A spike is not a feature.

Findings land in an [ADR](../adr/) or the [design doc](../design/architecture.md). Keep the notes here; do not promote a spike directory into a long-lived subsystem.

## BA-6 confirmation spike

Setup lives in [`zeroth-spike/`](../../zeroth-spike/) (Linear [42-5](https://linear.app/42-golems/issue/42-5/spike-setup-throwaway-repo-fixtures-resultsmd-template)). Measure gates in [RESULTS.md](RESULTS.md). The sandbox and session interfaces in the spike tree are the candidates to copy into `internal/` for M1 and M2. The implementations are throwaway.
